import React from 'react';
import NavBar from './NavBar/NavBar';
import './HomePage.css';

const HomePage = (props) => {
    return (
        <div>
            <div id="body">
                <div id="words">
                    <h1 className="home-title">STEPS BY STEPS</h1>
                    <h2>Saving the earth one step at a time</h2>
                </div>
            </div>
        </div>
    );
}

export default HomePage